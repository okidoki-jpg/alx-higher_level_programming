-- List all records of the table "second_table" of the
-- database "hbtn_0c_0", displaying both the score and the
-- name, ordering the records by score (top first)

SELECT `score`, `name`
FROM `second_table`
ORDER BY `score` DESC;
